# Override root .gitignore: the addon needs poetry.lock for reproducible Docker builds
!poetry.lock

# Runtime data (never commit): local fallback transcripts saved when persistence
# to Genesis fails, and audio recordings.
failed_transcripts/
recordings/
